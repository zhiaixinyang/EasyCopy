package com.mdove.easycopy.config;

import android.os.Environment;

public class ImageConfig {
    public static final String CONSTANT_IMAGE_PATH = Environment.getExternalStorageDirectory().getAbsolutePath()
            + "/easycopy/";
}
